android_app {
    name: "BackupRestore",
    // Only compile source java files in this apk.
    srcs: ["src/**/*.java"],
    sdk_version: "current",
    dex_preopt: {
        enabled: false,
    },
    optimize: {
        proguard_flags_files: ["proguard.flags"],
    },
}
